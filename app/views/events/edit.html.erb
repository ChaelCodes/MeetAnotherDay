<div class="container">
  <div class="section">
    <h1 class="title">Editing <%= @event.name %></h1>

    <%= render 'form', event: @event %>

    <%= link_to 'Show', @event %> |
    <%= link_to 'Back', events_path %>
  </div>
</div>
