<%= form_with(model: friendship) do |form| %>
  <% if friendship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
        <% friendship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :friend_id %>
    <%= form.text_field :friend_id %>
  </div>

  <div class="field">
    <%= form.label :buddy_id %>
    <%= form.text_field :buddy_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.select :status, Friendship.statuses.keys %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
